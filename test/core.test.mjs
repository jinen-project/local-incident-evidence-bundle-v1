import test from 'node:test';import assert from 'node:assert/strict';import os from 'node:os';import fs from 'node:fs/promises';import path from 'node:path';import {ingest} from '../src/core.mjs';test('missing event is not formed and journal is untouched',async()=>{const d=await fs.mkdtemp(path.join(os.tmpdir(),'lieb-t-')),c={__file:path.join(d,'c.json'),configuration_id:'x',principal:'x',scope:'x',authority_evidence:'x',storage_root:'data',destinations:{inbox:{type:'file',path:'inbox.txt'}}},r=await ingest({event:{event_id:'e'},config:c,root:path.join(d,'.lieb')});assert.equal(r.status,'NOT_FORMED');assert.equal(r.journal_event_created,false)})
